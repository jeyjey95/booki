https://github.com/jeyjey95/booki
*********************
quand "github" est modifié est qu'on peut l'écraser:
git pull origin main --allow-unrelated-histories
*************************

git add .
git commit -m “modif”

git push -m origin main